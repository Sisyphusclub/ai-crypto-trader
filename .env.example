# AI Crypto Trader - Environment Configuration
# Copy to .env and CHANGE ALL SECRET VALUES before running.
# NEVER commit .env to version control.
# NEVER use default values in production.

# ============================================================
# SECURITY SECRETS (MUST CHANGE - service will refuse to start with defaults)
# ============================================================
# Generate with: openssl rand -hex 32
JWT_SECRET=change-me-generate-with-openssl-rand-hex-32
MASTER_KEY=change-me-generate-with-openssl-rand-hex-32

# ============================================================
# Application Settings
# ============================================================
APP_ENV=dev

# Paper trading mode (recommended for initial testing)
PAPER_TRADING=true

# ============================================================
# Database & Cache
# ============================================================
# For local development (docker-compose handles internal networking)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/trader
REDIS_URL=redis://localhost:6379/0

# ============================================================
# Frontend
# ============================================================
NEXT_PUBLIC_API_URL=http://localhost:8000
